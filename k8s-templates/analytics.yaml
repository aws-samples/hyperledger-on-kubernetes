# Copyright 2018 Koinearth India Pvt. Ltd. , Inc. or its affiliates. All Rights Reserved.

apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: %DOMAIN%
  name: analytics-%ORG%
spec:
  replicas: 1
  strategy: {}
  selector:
    matchLabels:
       app: analytics
       role: client
       org: %ORG%
       name: analytics-%ORG%
  template:
    metadata:
      labels:
       app: analytics
       role: client
       org: %ORG%
       name: analytics-%ORG%
    spec:
     containers:
       - name: analytics-%ORG%
         image: koinearthindia/b2b-orders-common-services:0.4.0
         env:
         - name:  FABRIC_CA_SERVER_HOME
           value: /etc/hyperledger/fabric-ca
         ports:
          - containerPort: 3000
         volumeMounts:
          - mountPath: /app/certificates
            name: rca-data
          - mountPath: /app/config
            name: rca-scripts
     volumes:
       - name: rca-data
         persistentVolumeClaim:
             claimName: rca-%ORG%-pvc
       - name: rca-scripts
         persistentVolumeClaim:
             claimName: rca-scripts-%ORG%-pvc
     imagePullSecrets:
     - name: image-pull-secret

---
apiVersion: v1
kind: Service
metadata:
  namespace: %DOMAIN%
  name: analytics-%ORG%
spec:
 selector:
   app: analytics
   role: client
   org: %ORG%
   name: analytics-%ORG%
 type: NodePort
 ports:
   - name: endpoint
     protocol: TCP
     port: 7054
     targetPort: 7054
